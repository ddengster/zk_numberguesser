{
  "name": "guess_the_number",
  "version": "1.0.0",
  "description": "Guess the number via blockchain!",
  "main": "index.js",
  "scripts": {
    "build": "browserify ./frontend/app.js -o ./frontend/bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^4.5.0",
    "web3-eth": "^1.7.1"
  },
  "directories": {
    "test": "test"
  }
}
